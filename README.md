# KubernetesSubmissions

Submissions for the University of Helsinki's **DevOps with Kubernetes** MOOC
(2026 edition).

## Exercises

### Part 1

| Folder | Exercise | App | Image | Submit link |
|---|---|---|---|---|
| [`part1/1.1/`](./part1/1.1) | 1.1 — Getting started | `log_output` (Rust + tokio) | `tripplen63/log-output:1.1` | [tag `1.1`](https://github.com/tripplen23/KubernetesSubmissions/tree/1.1/part1/1.1) |
| [`part1/1.2/`](./part1/1.2) | 1.2 — Project v0.1 | `todo-app` (Rust + axum) | `tripplen63/todo-app:1.2` | [tag `1.2`](https://github.com/tripplen23/KubernetesSubmissions/tree/1.2/part1/1.2) |
| [`part1/1.3/`](./part1/1.3) | 1.3 — Declarative approach | `log_output` (manifests) | `tripplen63/log-output:1.3` | [tag `1.3`](https://github.com/tripplen23/KubernetesSubmissions/tree/1.3/part1/1.3) |
| [`part1/1.4/`](./part1/1.4) | 1.4 — Project v0.2 | `todo-app` (manifests) | `tripplen63/todo-app:1.4` | [tag `1.4`](https://github.com/tripplen23/KubernetesSubmissions/tree/1.4/part1/1.4) |
| [`part1/1.5/`](./part1/1.5) | 1.5 — Project, step 3 (HTML) | `todo-app` (HTML landing page) | `tripplen63/todo-app:1.5` | [tag `1.5`](https://github.com/tripplen23/KubernetesSubmissions/tree/1.5/part1/1.5) |
| [`part1/1.6/`](./part1/1.6) | 1.6 — Project, step 4 (NodePort) | `todo-app` (NodePort Service) | `tripplen63/todo-app:1.6` | [tag `1.6`](https://github.com/tripplen23/KubernetesSubmissions/tree/1.6/part1/1.6) |
| [`part1/1.7/`](./part1/1.7) | 1.7 — Project, step 5 (Ingress) | `log-output` (HTTP `/status`) | `tripplen63/log-output:1.7` | [tag `1.7`](https://github.com/tripplen23/KubernetesSubmissions/tree/1.7/part1/1.7) |
| [`part1/1.8/`](./part1/1.8) | 1.8 — Ingress instead of NodePort | `todo-app` (Ingress, ClusterIP) | `tripplen63/todo-app:1.8` | [tag `1.8`](https://github.com/tripplen23/KubernetesSubmissions/tree/1.8/part1/1.8) |
| [`part1/1.9/`](./part1/1.9) | 1.9 — More services | `ping-pong` (counter) + log-output | `tripplen63/ping-pong:1.9` | [tag `1.9`](https://github.com/tripplen23/KubernetesSubmissions/tree/1.9/part1/1.9) |
| [`part1/1.10/`](./part1/1.10) | 1.10 — Even more services (emptyDir) | `log-output` (writer + reader) | `tripplen63/log-output:1.10` | [tag `1.10`](https://github.com/tripplen23/KubernetesSubmissions/tree/1.10/part1/1.10) |
| [`part1/1.11/`](./part1/1.11) | 1.11 — Persisting data (PV/PVC) | `ping-pong` + `log-output` (shared PV) | `tripplen63/ping-pong:1.11`, `tripplen63/log-output:1.11` | [tag `1.11`](https://github.com/tripplen23/KubernetesSubmissions/tree/1.11/part1/1.11) |
| [`part1/1.12/`](./part1/1.12) | 1.12 — The project, step 6 (hourly image) | `todo-app` (hourly picture from Lorem Picsum, cached on PV) | `tripplen63/todo-app:1.12` | [tag `1.12`](https://github.com/tripplen23/KubernetesSubmissions/tree/1.12/part1/1.12) |
| [`part1/1.13/`](./part1/1.13) | 1.13 — The project, step 7 (todo UI) | `todo-app` (input ≤140 chars + Send button + hardcoded todos) | `tripplen63/todo-app:1.13` | [tag `1.13`](https://github.com/tripplen23/KubernetesSubmissions/tree/1.13/part1/1.13) |

### Part 2

| Folder | Exercise | App | Image | Submit link |
|---|---|---|---|---|
| [`part2/2.1/`](./part2/2.1) | 2.1 — Connecting pods | `ping-pong` + `log-output` (HTTP between pods, no shared volume) | `tripplen63/ping-pong:2.1`, `tripplen63/log-output:2.1` | [tag `2.1`](https://github.com/tripplen23/KubernetesSubmissions/tree/2.1/part2/2.1) |
| [`part2/2.2/`](./part2/2.2) | 2.2 — The project, step 8 (todo-backend) | `todo-app` (UI) + `todo-backend` (GET/POST /todos, in-memory) | `tripplen63/todo-app:2.2`, `tripplen63/todo-backend:2.2` | [tag `2.2`](https://github.com/tripplen23/KubernetesSubmissions/tree/2.2/part2/2.2) |
| [`part2/2.3/`](./part2/2.3) | 2.3 — Keep them separated (namespaces) | `ping-pong` + `log-output` moved to namespace `exercises` | `tripplen63/ping-pong:2.1`, `tripplen63/log-output:2.1` | [tag `2.3`](https://github.com/tripplen23/KubernetesSubmissions/tree/2.3/part2/2.3) |
| [`part2/2.4/`](./part2/2.4) | 2.4 — The project, step 9 (namespace) | `todo-app` + `todo-backend` moved to namespace `project` | `tripplen63/todo-app:2.2`, `tripplen63/todo-backend:2.2` | [tag `2.4`](https://github.com/tripplen23/KubernetesSubmissions/tree/2.4/part2/2.4) |
| [`part2/2.5/`](./part2/2.5) | 2.5 — Documentation and ConfigMaps | `log-output` reads ConfigMap file + env (namespace `exercises`) | `tripplen63/log-output:2.5`, `tripplen63/ping-pong:2.1` | [tag `2.5`](https://github.com/tripplen23/KubernetesSubmissions/tree/2.5/part2/2.5) |
| [`part2/2.6/`](./part2/2.6) | 2.6 — The project, step 10 (no hardcoded config) | `todo-app` + `todo-backend` config via ConfigMap/env (namespace `project`) | `tripplen63/todo-app:2.6`, `tripplen63/todo-backend:2.6` | [tag `2.6`](https://github.com/tripplen23/KubernetesSubmissions/tree/2.6/part2/2.6) |
| [`part2/2.7/`](./part2/2.7) | 2.7 — Stateful applications | `ping-pong` counter in Postgres StatefulSet (namespace `exercises`) | `tripplen63/ping-pong:2.7` | [tag `2.7`](https://github.com/tripplen23/KubernetesSubmissions/tree/2.7/part2/2.7) |
| [`part2/2.8/`](./part2/2.8) | 2.8 — The project, step 11 (todos in Postgres) | `todo-backend` stores todos in Postgres StatefulSet via Secret/ConfigMap (namespace `project`) | `tripplen63/todo-backend:2.8`, `tripplen63/todo-app:2.6` | [tag `2.8`](https://github.com/tripplen23/KubernetesSubmissions/tree/2.8/part2/2.8) |
| [`part2/2.9/`](./part2/2.9) | 2.9 — The project, step 12 (CronJob) | CronJob adds a "Read \<URL\>" todo every hour (Wikipedia random) | `tripplen63/todo-cron:2.9` + project `todo-app:2.6`, `todo-backend:2.8` | [tag `2.9`](https://github.com/tripplen23/KubernetesSubmissions/tree/2.9/part2/2.9) |
| [`part2/2.10/`](./part2/2.10) | 2.10 — The project, step 13 (request logging + monitoring) | request logging in backend; 140-char limit; Prometheus+Loki+Alloy+Grafana stack via Helm | `tripplen63/todo-backend:2.10` + project `todo-app:2.6`, `todo-cron:2.9` | [tag `2.10`](https://github.com/tripplen23/KubernetesSubmissions/tree/2.10/part2/2.10) |

### Part 3

| Folder | Exercise | App | Image | Submit link |
|---|---|---|---|---|
| [`part3/3.1/`](./part3/3.1) | 3.1 — Pingpong GKE | `ping-pong` app on GKE, exposed via LoadBalancer | `gcr.io/dwk-gke-506208/ping-pong:3.1` | [tag `3.1`](https://github.com/tripplen23/KubernetesSubmissions/tree/3.1/part3/3.1) |
| [`part3/3.2/`](./part3/3.2) | 3.2 — Back to Ingress (GKE) | "Log output" + "Ping-pong" on GKE, exposed via Ingress (NodePort services) | `gcr.io/dwk-gke-506208/ping-pong:3.2`, `gcr.io/dwk-gke-506208/log-output:3.2` | [tag `3.2`](https://github.com/tripplen23/KubernetesSubmissions/tree/3.2/part3/3.2) |
| [`part3/3.3/`](./part3/3.3) | 3.3 — To the Gateway (GKE) | "Log output" + "Ping-pong" via Gateway (Gateway + HTTPRoute, ClusterIP services) | `gcr.io/dwk-gke-506208/ping-pong:3.3`, `gcr.io/dwk-gke-506208/log-output:3.3` | [tag `3.3`](https://github.com/tripplen23/KubernetesSubmissions/tree/3.3/part3/3.3) |
| [`part3/3.4/`](./part3/3.4) | 3.4 — Rewritten routing (GKE Gateway API) | "Log output" + "Ping-pong" via rewritten Gateway routing | `gcr.io/dwk-gke-506208/ping-pong:3.4`, `gcr.io/dwk-gke-506208/log-output:3.4` | [tag `3.4`](https://github.com/tripplen23/KubernetesSubmissions/tree/3.4/part3/3.4) |
| [`part3/3.5/`](./part3/3.5) | 3.5 — The project, step 14 (Kustomize + GKE) | `todo-app` + `todo-backend` + Postgres + CronJob deployed to GKE with Kustomize | `gcr.io/dwk-gke-506208/todo-app:3.5`, `todo-backend:3.5`, `todo-cron:3.5`, `postgres:16` | [tag `3.5`](https://github.com/tripplen23/KubernetesSubmissions/tree/3.5/part3/3.5) |
| [`part3/3.6/`](./part3/3.6) | 3.6 — The project, step 15: Deployment pipeline (GitHub Actions + OIDC) | full CI/CD: build → publish to Artifact Registry → deploy to GKE on every push | `europe-north1-docker.pkg.dev/dwk-gke-506208/my-repository/{todo-app,todo-backend,todo-cron}:<branch>-<sha>` | [tag `3.6`](https://github.com/tripplen23/KubernetesSubmissions/tree/3.6/part3/3.6) |
| [`part3/3.7/`](./part3/3.7) | 3.7 — The project, step 16: separate environment for each branch | pipeline deploys each git branch to a namespace named after the branch (`main` keeps `project`) | `europe-north1-docker.pkg.dev/dwk-gke-506208/my-repository/{todo-app,todo-backend,todo-cron}:feat37-<sha>` | [tag `3.7`](https://github.com/tripplen23/KubernetesSubmissions/tree/3.7/part3/3.7) |
| [`part3/3.8/`](./part3/3.8) | 3.8 — The project, step 17: deleting a branch deletes the environment | new workflow `delete-environment.yaml` (`on: delete` → `kubectl delete namespace`, branch-only guard) | — | [tag `3.8`](https://github.com/tripplen23/KubernetesSubmissions/tree/3.8/part3/3.8) |
| [`part3/3.9/`](./part3/3.9) | 3.9 — DBaaS vs DIY (GKE features) | pros/cons comparison: managed Cloud SQL vs own Postgres + PVC (init work/cost, maintenance, backups) | — (writing exercise) | [tag `3.9`](https://github.com/tripplen23/KubernetesSubmissions/tree/3.9/part3/3.9) |
| [`part3/3.10/`](./part3/3.10) | 3.10 — The project, step 18: scheduled backup to Google Object Storage | CronJob (dump + upload, two containers) backs up the todo DB every 24 h to a GCS bucket; the upload authenticates with Workload Identity (no key — the org blocks SA keys) | `gcr.io/dwk-gke-506208/postgres:16`, `gcr.io/google.com/cloudsdktool/cloud-sdk:latest` | [tag `3.10`](https://github.com/tripplen23/KubernetesSubmissions/tree/3.10/part3/3.10) |
| [`part3/3.11/`](./part3/3.11) | 3.11 — The project, step 19: resource requests and limits | every container gets `resources` (`todo-app`/`todo-backend`/`todo-cron` 50m/64Mi → 200m/256Mi, `postgres` 100m/256Mi → 500m/512Mi); values chosen from `kubectl top` measurements, plus the requests ≤ limits invariant and OOMKilled/Pending experiments | `europe-north1-docker.pkg.dev/dwk-gke-506208/my-repository/todo-{app,backend,cron}:3.11` | [tag `3.11`](https://github.com/tripplen23/KubernetesSubmissions/tree/3.11/part3/3.11) |
| [`part3/3.12/`](./part3/3.12) | 3.12 — The project, step 20: application logs in GKE | GKE monitoring/logging verified, the project's logs found in Logs Explorer / `gcloud logging read` (filter `namespace_name="project"`, `container_name="todo-backend"`), a screenshot of `[req] POST /todos -> 201 Created` when a todo is created, plus the Observability tab and the log-metric/alert/uptime tour | `europe-north1-docker.pkg.dev/dwk-gke-506208/my-repository/todo-{app,backend,cron}:3.12` | [tag `3.12`](https://github.com/tripplen23/KubernetesSubmissions/tree/3.12/part3/3.12) |
### Part 4

| [`part4/4.1/`](./part4/4.1) | 4.1 — Readiness probe | A rolling update that "succeeds" while the app is broken, then `readinessProbe` on the ping-pong and log-output applications so they are ready only when the database / the ping-pong connection is up — tested exactly as the exercise asks (apply everything but the Postgres statefulset: `0/1` + `1/2`, then `1/1` + `2/2`), plus rolling back a stalled update | `europe-north1-docker.pkg.dev/dwk-gke-506208/my-repository/{flaky-app,ping-pong,log-output}:4.1` | [tag `4.1`](https://github.com/tripplen23/KubernetesSubmissions/tree/4.1/part4/4.1) |
| [`part4/4.2/`](./part4/4.2) | 4.2 — The project, step 21: probes and a "break" button | `readinessProbe` on the project's two Deployments (`todo-backend /healthz` = a fresh Postgres connection + `SELECT 1`, `todo-app /healthz` = own state + the backend), plus a "Break the app" button that makes the pod not Ready (out of the Service, the app stops answering) and lets the liveness probe restart the container healthy again; a bad update rolled back with `rollout undo` / `--to-revision` | `europe-north1-docker.pkg.dev/dwk-gke-506208/my-repository/{todo-app,todo-backend}:4.2` | [tag `4.2`](https://github.com/tripplen23/KubernetesSubmissions/tree/4.2/part4/4.2) |
| [`part4/4.3/`](./part4/4.3) | 4.3 — Prometheus | The monitoring stack installed with Helm on a cluster that cannot reach quay.io / registry.k8s.io / ghcr.io (nine images mirrored first, including the `config-reloader` the operator injects at runtime), port-forwarded through the **Service**, and the exercise's query — `count(kube_pod_info{namespace="monitoring", created_by_kind="StatefulSet"})` → **2** — checked in the GUI and through the HTTP API, with `count by (created_by_name)` for the breakdown, three follow-up queries, and the `kube_pod_container_status_restarts_total` counter that a canary analysis differences over two minutes | `europe-north1-docker.pkg.dev/dwk-gke-506208/my-repository/{prometheus,prometheus-operator,prometheus-config-reloader,alertmanager,node-exporter,kube-state-metrics,k8s-sidecar,kube-webhook-certgen,grafana}:91.2.3-images` | [tag `4.3`](https://github.com/tripplen23/KubernetesSubmissions/tree/4.3/part4/4.3) |
| [`part4/4.4/`](./part4/4.4) | 4.4 — Your canary | Argo Rollouts (mirrored image, CRDs applied `--server-side`) driving a canary whose `AnalysisTemplate` sums the CPU of the whole namespace — `scalar(sum(rate(container_cpu_usage_seconds_total{namespace="canary", container!=""}[1m])))`. A release that answers `/healthz` 200 while burning half a core fails its first measurement (0.3476 > 0.3) and is reverted; a release that is measurably cheap passes 8/8 measurements ≈ 0.0003 cores inside the exercise's 5 minutes; a threshold set too low (0.0002) blocks even the good release, so the application is never updated | `europe-north1-docker.pkg.dev/dwk-gke-506208/my-repository/{ping-pong:4.4,argo-rollouts:v1.10.0,prometheus:v3.14.0-distroless,prometheus-operator:v0.94.0,prometheus-config-reloader:v0.94.0,node-exporter:v1.12.1-distroless,kube-state-metrics:v2.20.0,kube-webhook-certgen:1.8.8}` | [tag `4.4`](https://github.com/tripplen23/KubernetesSubmissions/tree/4.4/part4/4.4) |
| [`part4/4.5/`](./part4/4.5) | 4.5 — The project, step 22: a Done field, and two ways to update | The project's todos get a `done` field set through the exercise's **`PUT /todos/<id>`** (plus a `DELETE /todos/<id>` for the extra Delete button): an HTML form can only POST, so the app is the adapter that speaks REST to the backend, and the SQL is a single `UPDATE … RETURNING`. Then the two strategies the chapter teaches, both measured: **Recreate** — this app *must* use it, its `image-claim` is ReadWriteOnce, and a rolling update deadlocks with `Multi-Attach error` — costing 16 of 227 requests during the swap; and **BlueGreen** on the backend with Argo Rollouts, where the new version runs beside the old one, users keep getting `{"version":"v1"}` while only the preview serves `v2`, and one `promote` switches everybody. Also fixes the front page's picture: `picsum.photos` is unreachable from this private cluster, so the lab points `IMAGE_URL` at a reachable JPEG and serves the last cached copy when a refresh fails — verified `200 image/jpeg 44891 bytes` inside the cluster | `europe-north1-docker.pkg.dev/dwk-gke-506208/my-repository/{todo-app,todo-backend}:4.5` | [tag `4.5`](https://github.com/tripplen23/KubernetesSubmissions/tree/4.5/part4/4.5) |
| [`part4/4.6/`](./part4/4.6) | 4.6 — The project, step 23: NATS, a broadcaster and a queue group | the backend publishes one event per change to NATS (`created`/`done`/`undone`/`deleted`); `NATS_URL` unset simply means no messaging and a failed publish never fails the request. A **broadcaster** subscribes and forwards each event to a chat service in the exercise's `{"user":"bot","message":"…"}` shape (`CHAT_FORMAT` switches to Discord's `{"content"}` or Slack's `{"text"}`), and a **queue group** is what makes six replicas safe: measured 10 messages → 10 with the group, 60 without it, and 70 mid-rollout because the previous generation still counts as one member. Core NATS is *at most once* — a message nobody hears is gone, and JetStream is the answer when that is not acceptable. The lab installs NATS with its exporter, adds the scrape job, and provisions a four-panel Grafana dashboard from a labeled ConfigMap | `europe-north1-docker.pkg.dev/dwk-gke-506208/my-repository/{todo-backend,broadcaster,chat-sink}:4.6` (+ mirrored `nats-server`, `nats-box`, `nats-reloader`, `nats-exporter`) | [tag `4.6`](https://github.com/tripplen23/KubernetesSubmissions/tree/4.6/part4/4.6) |
| [`part4/4.7/`](./part4/4.7) | 4.7 — Baby steps to GitOps | The chapter's pull pipeline, with one thing changed by necessity: this cluster cannot reach `github.com` (nor gitlab/codeberg/bitbucket — measured from a pod, `000 in 8s`), and ArgoCD clones over the network, so the repository runs **inside** the cluster as a Gitea. The app's image tag lives in a Kustomize **base + prod overlay** (`PROJECT/IMAGE` placeholder, `namePrefix: prod-`, `kustomize edit set image` as the release step); the `Application` is created twice — once by hand in the UI (Auto-Sync / Prune / Self-Heal, Create Namespace) so the badges and the `Deployment → ReplicaSet → Pod` tree are readable, then declaratively from `application.yaml` pointing at `overlays/prod`. Two proofs, both measured: `kubectl scale --replicas=5` is reverted to `1/1` by `selfHeal`, and one commit (`APP_VERSION=v2`, deployed as revision `33bf607`) rolls the app out — `version: <b>v2</b>` — with nobody touching the cluster. The lab documents the traps it hit: Gitea's `ReadWriteOnce` volume needs `strategy: Recreate` (the `Multi-Attach` deadlock of 4.5), `argocd-application-controller` in `ImagePullBackOff` is why `argocd-initial-admin-secret` does not exist, the chapter's `LoadBalancer` service is unreachable on a private-node cluster, and a push-created repository comes out **private** — which ArgoCD reads anonymously, so it fails with *authentication required* until it is made public | `europe-north1-docker.pkg.dev/dwk-gke-506208/my-repository/{log-output:4.7,gitea:1.24,argocd:v3.5.3,dex:v2.45.1,redis:8.2.3-alpine}` | [tag `4.7`](https://github.com/tripplen23/KubernetesSubmissions/tree/4.7/part4/4.7) |
| [`part4/4.10/`](./part4/4.10) | 4.10 — The project, the grande finale: the code and the configuration in two repositories | The exercise's whole content: **this repository holds the application code and the release workflow, and [`tripplen23/dwk-config`](https://github.com/tripplen23/dwk-config) holds every Kubernetes object** — twelve base manifests, a `staging` and a `production` overlay, and the two ArgoCD `Application`s that point at them (25 files typed in total; the four Dockerfiles are 4.9's, copied forward unchanged). The split is countable: `git ls-tree` finds **0** manifests here and **0** Rust there. The release pipeline crosses repositories: a push to `main` filtered by `paths: part4/4.10/**` builds four images tagged with the commit SHA, rewrites `overlays/staging`'s four `newTag` lines and commits them into the config repository, while a tag `4.10.*` does the same to `overlays/production` with the version as the tag — and then **carries that tag name into the config repository**, which is the step that matters, because production's `targetRevision: >=4.10.0` is resolved against tags *in the repository ArgoCD reads*, not in the one that was tagged. ArgoCD arrives with `kubectl apply --server-side --force-conflicts` (a plain apply dies mid-install on `metadata.annotations: Too long: may not be more than 262144 bytes` for the `applicationsets.argoproj.io` CRD, leaving `Application`s that sync while CRDs are quietly missing). Measured on the real cluster: staging followed a commit in **one ArgoCD poll** (180 s) while production stayed pinned to the constraint until the tag landed, then rolled out `broadcaster 6/6`, `todo-app`, `todo-backend`, `chat-sink`, `postgres-ss-0` and `my-nats-0` all `1/1`; the config repository's history begins with the hand-typed commits `bee4578` → `3ba347c` → `6405949` → `fc4fc2e` and then grows a commit per release, and the registry holds images `4.10.0` and `4.10.1` in `my-repository`. Traps written where they bite: `paths:` is not evaluated for tag events (so a tag releases even with an empty diff), a workflow outside the repository root never runs and fails silently, `id-token: write` is what WIF mints from, a third-party commit action crashes on this job and the release step is plain `git` instead, and a re-released tag commits nothing — `git commit` exits 1 on an overlay that already carries the version. | `europe-north1-docker.pkg.dev/dwk-gke-506208/my-repository/{todo-app,todo-backend,broadcaster,chat-sink}:4.10.0` and `:4.10.1` (+ `postgres:16`, `nats:2.14.6-alpine`, `google/cloud-sdk:slim`) | [tag `4.10.1`](https://github.com/tripplen23/KubernetesSubmissions/tree/4.10.1/part4/4.10) · [config repo](https://github.com/tripplen23/dwk-config) |
**Part 4's project lives in two repositories**: this one for the code and the release workflow, [`tripplen23/dwk-config`](https://github.com/tripplen23/dwk-config) for the manifests and the release history. Exercise 4.10 is where the split happens; the rows above it predate it, and their manifests are still in this repository as they were in the course's earlier parts.
| [`part4/4.8/`](./part4/4.8) | 4.8 — The project, step 24: the project through GitOps | The whole project — Postgres, the todo frontend and backend, the broadcaster, the chat sink and NATS — declared as a Kustomize **base + `overlays/prod`** and reconciled by ArgoCD, so a commit is the only thing that changes the cluster. The repository is the submission's own **GitHub repository** (config under `part4/4.8/config/`, read anonymously by the cluster), which needed one infrastructure repair first: these private nodes had **no Cloud NAT**, so every public git host timed out while only Google services answered — one router + NAT later `git ls-remote` from inside the cluster returns the repo's HEAD, and images pull straight from `quay.io`/`ghcr.io`/`ecr`/Docker Hub instead of being mirrored. Every image is a placeholder (`PROJECT/TODO-APP`, …) that the overlay rewrites with `images:`, which is what makes `kustomize edit set image …` the release step and the tag the state. Both proofs measured with nobody touching the cluster: a release commit was picked up by ArgoCD's own 180-second poll after **144 s**, the container's `VERSION` had changed by **150 s** and the new pod was ready at **201 s**; `kubectl scale deploy/todo-app --replicas=3` was back to the repository's **1** in **5 s**. The traps are written down where they bite: a namespace that still holds a previous lab's NATS makes the first sync fail on immutable fields (`Service … clusterIPs may not change once set`, `StatefulSet … updates to statefulset spec … are forbidden`) and ArgoCD never retries that revision; a strategic-merge patch must repeat the namespace its target carries or `kustomize build` refuses to render; `kustomize edit set image` matches the placeholder *name*, not the registry path | `europe-north1-docker.pkg.dev/dwk-gke-506208/my-repository/{todo-app,todo-backend,broadcaster,chat-sink}:4.8` (+ `postgres:16`, `nats:2.14.6-alpine` straight from Docker Hub) | [tag `4.8`](https://github.com/tripplen23/KubernetesSubmissions/tree/4.8/part4/4.8) |
| [`part4/4.9/`](./part4/4.9) | 4.9 — The project, step 25: two environments, one repository | The project split into **staging** and **production**, each in its own namespace, off one base and two overlays, with the difference between the environments countable: a namespace, a replica count, two environment variables and one extra object. Staging follows `refs/heads/main`, so every commit is a release; production follows the semver constraint **`>=4.9.0`**, which matches tags and nothing else, so the tag is the promotion. ArgoCD's UI refuses to create the production `Application` before such a tag exists — `InvalidSpecError: Unable to generate manifests … unable to resolve '>=4.9.0' to a commit SHA` — which is the constraint doing its job; creating it from the file instead works, and the card sits at `Unknown` with a `ComparisonError` until Proof 2's tag clears it. Measured with nobody touching the cluster: a commit was in staging **186 s** later while production's revision stayed the constraint, and the tag moved production in one poll. The environment difference is a feature flag, not a second program — `BROADCASTER_LOG_ONLY` makes staging's broadcaster log `[log] #1 …` and forward nothing (`/count` stayed **0**, production's reached **1**), and the backup `CronJob` exists only in production: `dump written: 2020 bytes` uploaded to `gs://dwk-todo-backups-tripplen23/…`, which needed `roles/storage.objectAdmin` rather than `objectCreator` because `gcloud storage cp` reads the object back. Secrets and the `backup-sa` ServiceAccount are applied by hand outside ArgoCD, and both Applications' inventories contain **0** of them. The traps are written where they bite: the UI validates manifests before it creates an Application, a failed `CronJob` turns its whole Application `Degraded`, `--member` in the IAM binding is scoped to one namespace (`ns/production/sa/backup-sa`), and `git tag` without `-a` is lightweight — `ls-remote` prints it without a `^{}` line | `europe-north1-docker.pkg.dev/dwk-gke-506208/my-repository/{todo-app,todo-backend,broadcaster,chat-sink}:4.9` (+ `postgres:16`, `nats:2.14.6-alpine`, `google/cloud-sdk:slim`) | [tag `4.9`](https://github.com/tripplen23/KubernetesSubmissions/tree/4.9/part4/4.9) |

## Prerequisites

A running Kubernetes cluster with `kubectl` connected to it. Quickstart with k3d:

```bash
curl -s https://raw.githubusercontent.com/k3d-io/k3d/main/install.sh | bash
k3d cluster create
kubectl cluster-info
```
